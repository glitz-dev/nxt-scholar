const { Component } = require("react");
const { ToastContainer } = require("react-toastify");

function Notify()
{
    return(
    <>
    <Component {...pageProps} />
    <ToastContainer/>
    </>
    );
}

export default Notify;